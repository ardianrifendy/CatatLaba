// User-visible strings for the shared glass primitives (Bahasa Indonesia).
// Re-exported through the ui-text index; components import from here only —
// never hardcode UI strings inside components.
export const controlsText = {
  /** Placeholder for search inputs inside pickers. */
  search: 'Cari…',
  /** Shown when a picker's search filter matches no options. */
  noResults: 'Tidak ada pilihan yang cocok',
  /** Default cancel action label. */
  cancel: 'Batal',
  /** Default close action label. */
  close: 'Tutup',
  /** Accessible name for the toast notification region. */
  notifications: 'Notifikasi',
} as const
